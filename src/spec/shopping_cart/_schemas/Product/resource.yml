model: Product
refs:
  Product:
    model: Product
    plural: Products
    singular: Product
    references:
      primaryKey:
        prop: id
        type: integer
schema: !include schema.json
uiSchema: !include uiSchema.json
attributes:
  findAll:
  - name
  - price
actions: !include ../actions.yml
