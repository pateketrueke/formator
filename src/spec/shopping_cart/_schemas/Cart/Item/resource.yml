model: CartItem
refs:
  CartItem:
    model: CartItem
    plural: CartItems
    singular: CartItem
    references:
      primaryKey:
        prop: id
        type: integer
  cart_id:
    rel: belongsTo
    model: Cart
    plural: Carts
    singular: Cart
    references:
      primaryKey:
        prop: id
        type: integer
      foreignKey:
    requiredProps:
    - id
    - items
    uiSchema: {}
  product_id:
    rel: belongsTo
    model: Product
    plural: Products
    singular: Product
    references:
      primaryKey:
        prop: id
        type: integer
      foreignKey:
    requiredProps:
    - id
    - name
    - price
    uiSchema:
      name:
        ui:title: OK
schema: !include schema.json
uiSchema: !include uiSchema.json
actions: !include ../../actions.yml
